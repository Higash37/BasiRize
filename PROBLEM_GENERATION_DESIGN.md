# PROBLEM_GENERATION_DESIGN

## 現在の構成

BasiRizeは、問題タイプの取得と問題生成をブラウザ内のTypeScriptで行う。
DB・認証・秘密情報を扱わないため、問題生成のためのHTTP APIは使用しない。

```text
Reactの画面
  ↓ 同期関数を呼ぶ
problem-generation/index.ts
  ↓
問題タイプ台帳・各問題生成器
  ↓
Problem[]
```

## 公開する関数

画面は `src/problem-generation/index.ts` から次の関数だけを利用する。

- `getProblemTypes(level?)`: 問題タイプの一覧を返す
- `getProblemTypeById(id)`: IDに一致する問題タイプを返す
- `generateProblems(typeId, count)`: 重複しない問題を生成する

内部の生成器や計算クラスは画面から直接呼ばない。

## ディレクトリの責務

| ファイル        | 責務                                   |
| --------------- | -------------------------------------- |
| `types.ts`      | 問題と問題タイプの型                   |
| `operator.ts`   | 四則演算と優先順位                     |
| `expression.ts` | 数値・計算式・括弧の表示               |
| `generator.ts`  | 乱数、重複排除、試行回数の上限         |
| `arithmetic.ts` | 四則演算、割り算                       |
| `fraction.ts`   | 分数の約分と四則演算                   |
| `polynomial.ts` | 多項式・因数の文字列表現               |
| `algebra.ts`    | 同類項、方程式、展開、因数分解、平方根 |
| `registry.ts`   | 全問題タイプと生成器の対応             |

## 維持する制約

- 問題数は1〜200の整数
- 同じ問題文を1つのプリントへ重複させない
- 指定数の100倍試しても集まらなければエラーにする
- URLから受け取った問題タイプと数値を検証する
- 問題生成の失敗は画面で案内し、詳細をConsoleへ残す

## バックエンドを再検討する条件

次の要件が生じた場合は、問題生成やデータ管理をサーバーへ戻すことを検討する。

- DBへ問題や利用者情報を保存する
- ログイン・権限管理を追加する
- 生徒へ答えや生成規則を公開できない
- 利用回数の制限や課金をサーバー側で保証する
- ブラウザでは負荷が高い処理を追加する
