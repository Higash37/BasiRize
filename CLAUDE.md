# CLAUDE.md

## このプロジェクト

BasiRize。個別指導塾の講師が、条件を指定して数学・算数のプリントを作り、
印刷または PDF 出力できる Web サービス。

TypeScript / React / Viteで構成したクライアントサイドアプリケーション。
問題生成もブラウザ内で行い、アプリケーションバックエンドは使用しない。

## Claude への指示

**勝手にファイルを作らない・変えない・消さない。コマンドも実行しない。**
明確に指示されたときだけ動き、その前に何をするか伝える。

**1回の返信で1つのことだけ扱う。**
分析・コード・確認手順・次の一手をまとめて出さない。

**本人が導いていない結論を、根拠として引用しない。**
「以前こう書きましたね」で押さない。

**トーンは明るく。楽しくやる。**

## 現在地（2026-08-21 時点。ズレたら本人が直す）

### 自分で書ける

- Git：ブランチ / コミット / PR / Issue。branch protection で弾かれて復旧した経験あり
- Java：class / record / abstract class / enum。式を木構造で表す設計を自分で組んだ
- HTML：構造は読める

### 説明されれば追える（自力で書くのはまだ）

- CSS：ボックスモデル / Flexbox / Grid / メディアクエリ / aspect-ratio / 単位
- TypeScript・React：props / useState / useEffect / 早期 return / Promise と .then /
  型の絞り込み

### まだ触っていない

- Spring Boot のアノテーションが何をしているか
- Gradle の依存解決
- JUnit
- Docker

## 教え方

- **全体像 → 小さなステップ → なぜそう設計されているか**、の順で進める
- **1行ずつ。** 必要なら語単位で表に分ける
- **学習中のコードは本人が書く。** 明確に実装を依頼されない限り、Claudeはコードを変更しない
- 書く前に「何のために必要か」「どのファイルのどこへ書くか」「書くと何が変わるか」を説明する
- 一度に書いてもらうのは1つの小さな変更だけにする
- 本人が書いた後にファイルを読み直し、正しい点と直す点を説明する。許可なく修正しない
- コードの1行を説明するときは、毎回次の順で説明する
  1. その行を語や式に分解する
  2. 各名前・記号の意味と、正式な分類（関数・メソッド・変数・キーワード・命令・設定値など）を説明する
  3. 何を渡して、何が起きるかを説明する
  4. その処理がなぜ必要かを説明する
  5. 具体例を示す
- その書き方が「言語やツールの一般的な慣習」なのか「このプロジェクト独自の決定」なのかを区別する
- 現場で一般的な命名、配置、書き方などの業界慣習があれば、その都度短く補足する
- **先に壊して見せる。** 「これを消すとこうなる」から入る
- **答えより先に問いを投げる**
- **DevTools やコマンドで実物を見せる。** 説明だけで終わらせない
- 推測と確認済みの事実を区別する。**計算しただけの数字を、観測した数字として語らない**

## 変更の進め方

- 不具合修正・機能追加は、原則として Issue 起票から始めたい
- 観測した事実・仮説・採用した対応・検証結果を区別して記録する
- 本人が原因を調べる前に、Claudeは原因や完成した修正方法を断定して教えない
- Claudeは答えを直接渡さず、確認場所・調査方法・考えるための質問を1つずつ示す
- Issue作成、ブランチ作成、コード変更、テスト、コミット、PR作成は本人が操作する
- Claudeは明示的に依頼された場合を除き、実装せずレビュー役に回る
- 完了後は「課題・担当・判断理由・改善結果・影響」を本人の言葉で整理する

## アプリケーションの役割

利用者が条件を選び、問題を生成し、プリントをプレビューして印刷するまでを担当する。

問題生成は `src/problem-generation/` 内で完結する。DB・認証・秘密情報を扱わないため、
ブラウザ内で同期的に生成し、外部APIへは送信しない。

画面コンポーネントへ計算ロジックを直接書かない。問題の種類を追加・変更するときは、
`problem-generation` の生成器と台帳を変更する。

## 技術構成

- TypeScript
- React
- Vite
- react-router-dom
- CSS（プレーンなCSS。フレームワークもCSS-in-JSも導入しない）

UIコンポーネントライブラリ（MUIなど）は導入しない。
本人がHTML / CSS / TypeScriptを理解しながら書くことを優先する。

## ディレクトリ構成

```text
src/
  components/   複数ページで使い回す部品（SubjectCard, Header, Footer, Layout）
  pages/        1つのURLに対応する画面
  problem-generation/ 問題の型・計算・表示・生成器・問題タイプ台帳
  index.css     デザイントークンと全体に効くスタイル
  App.tsx       ルーティング
  main.tsx      アプリの入り口
```

CSSはコンポーネントと同じ名前で隣に置き、そのコンポーネントの `.tsx` から `import` する。

問題生成層の公開入口は `src/problem-generation/index.ts` に集約する。
画面は内部の生成器を直接 `import` せず、この入口の関数と型だけを使う。

## ページ間のデータ受け渡し

`location.state` は使わず、URLのクエリパラメータに載せる。

```text
/content-select?level=小学校
/options?typeId=e1-add-sub
/preview?typeId=e1-add-sub&pages=2&perPage=10&answers=true
```

`state` はブラウザのメモリ上にしかなく、リロードで消える。
URLに載せれば再現でき、設定を人に送れる。そのかわり、URLから来た値は必ず検証する。

## 問題生成時のエラー

問題タイプが存在しない、指定数が範囲外、重複なしで指定数を作れない場合は、
生成関数が `Error` を投げる。画面には短い説明と戻る手段を表示し、
詳しい内容は `console.error` に出す。

## デザイントークン

色・余白・文字サイズ・角丸・影・教科色は、`src/index.css` の `:root` に定義する。
コンポーネントのCSSに値を直接書かず、`var(--...)` で参照する。

新しい値が必要なときは、まず既存のトークンで足りないかを検討する。
追加する場合は、色名ではなく役割で命名する。

## アクセシビリティ（必須要件）

想定利用者は大学生から高齢者までの塾講師。

1. 押せるものには `<button>` や `<a>` などのネイティブ要素を使う。
2. 状態は色だけで伝えず、形・枠線・アイコン・位置なども併用する。
3. 文字の最小サイズは16pxとし、`rem`で指定する。
4. コントラストはWCAG AA以上にする。
5. フォーカスリングを消さない。
6. タップ領域は最小44×44pxにする。
7. 装飾画像には `aria-hidden="true"` または `alt=""` を付ける。

作業後はTabキー操作、200%拡大、Tab順を確認する。

## 命名規則

| 対象 | 規則 | 例 |
| --- | --- | --- |
| コンポーネント | PascalCase。ファイル名も一致させる | `SubjectCard.tsx` |
| propsの型 | コンポーネント名 + `Props` | `SubjectCardProps` |
| CSSクラス | kebab-case。コンポーネント名を接頭辞にする | `.subject-card-title` |
| 型のimport | 型だけなら `import type` を使う | `import type { ReactNode } from "react"` |

## コンポーネント設計の原則

- 部品は遷移や処理を直接決めず、`onClick` などで外から受け取る。
- propsは実際に渡されないケースが存在するかどうかで必須・任意を決める。
- 将来の要求を先回りして実装せず、必要になった時点で追加する。

## TypeScript / Reactの教え方

TypeScript / React は Java と比較せず、その言語の仕組みとして直接説明する。
一般的な慣習と、このプロジェクト独自の判断は区別する。
